# Service Decomposition Plan

This is an evidence-led plan; no production extraction is retained by the baseline tier.

| Current file | Observed size/calls | Cohesive responsibilities to investigate | Safe extraction order | Main risk |
|---|---:|---|---|---|
| `itineraries.service.ts` | 4,387 lines / 613 DB-call matches | create/update persistence; route/rebuild; manual/auto fit; hotel/vehicle/vendor pricing; confirmation/cancellation | input normalization -> collaborator callback wiring -> guide assignment policy -> guide assignment writes -> vehicle status -> vehicle orchestration -> plan persistence -> activity availability -> confirmed invoice/pluck read -> manual-fit timeline policy -> manual-fit removal explanation -> matrix-preview timeline policy -> manual-fit route policy -> manual-fit travel replica/saved-rule legs -> manual-fit route-matrix persistence -> manual-fit operating hours -> manual-fit validation -> manual-fit schedule attempts -> manual-fit candidate simulation -> manual-fit candidate search -> manual-fit candidate data -> manual hotspot row persistence -> manual hotspot schedule state -> manual hotspot row timing -> manual hotspot overlap -> manual hotspot conflict persistence -> route hotspot rebuild -> hotel cancellation -> hotel room category -> route optimization -> activity impact -> transport formatting -> activity pricing -> activity timing -> activity -> smart activity -> hotspot workflow -> hotspot deletion -> selection workflow -> quote context -> confirmation -> hotel confirmation support -> hotel prebook -> hotel booking fulfillment -> confirmed-plan copy -> cancellation -> listing -> voucher reads -> manual matrix -> manual preview -> manual mutation -> manual fit matrix planning -> exact-anchor rebuild -> low-priority removal -> matrix-safe insertion -> preview timeline application -> route-leg cache -> manual batch -> insertion fit -> progressive removal -> adaptive insertion -> matrix-rescheduled preview -> matrix merge -> confirmed-details projection -> route timing -> manual-fit geometry -> route/rebuild -> pricing | transaction and shared mutable state |
| `timeline.builder.ts` | 6,035 lines / 10 DB-call matches | route processing; travel legs; operating hours/cutoffs | pure timeline policy tests -> candidate policy -> input loader -> build-context hydration -> direct service delegation -> Day-1 fallback -> route-hotspot selection -> route-hotspot planning -> manual/same-city placement ordering -> destination reservation -> carry-forward attachment -> matrix autobuild -> candidate reordering -> Day-1 candidate gate -> Day-1 cutoff/master admission -> Day-1 travel projection -> arrival/hotel decisions -> hotel-first insertion -> non-hotel cutoff -> policy services | ordering/timing parity |
| `itinerary-details.service.ts` | 5,910 lines / 61 DB-call matches | data loading; response assembly; hotel details; activity/guide projections | read loaders -> timeline presentation -> read loaders -> response assembly | response shape and duplicate queries |
| frontend `HotelList.tsx` | 3,586 lines / component and display-policy logic | hotel selection UI; supplier normalization; rate/inclusion presentation; voucher actions | pure normalization -> selection state -> room/rate presentation -> voucher actions | supplier-value and rendered-state parity |
| `vendors.service.ts` | 3,478 lines / 161 DB-call matches | vendor CRUD; vehicles/slabs; local/outstation price books; permits; branches/lookups | lookup/read-only price-book query -> CRUD groups -> writes | shared Prisma model assumptions |
| `vehicle-calculation.helpers.ts` | 3,448 lines / vehicle pricing and distance policy | local/outstation pricing; stored-location reads; toll/parking; distance/duration; vehicle cost projection | pure pricing/slab policy -> location reads -> charge projection -> orchestration | pricing parity and mutable cache state |
| `hotels.service.ts` | 2,945 lines / 139 DB-call matches | hotel CRUD; rooms/rate plans; amenities; price books; provider sync | pure normalization -> room/rate-plan reads -> writes -> providers | provider and transaction coupling |
| `activities.service.ts` | 2,607 lines / 94 DB-call matches | CRUD; timeslots; price book; reviews; provider/storefront | pure mapping -> read groups -> transactional writes | upload and booking side effects |
| `locations.service.ts` | 2,521 lines / 68 DB-call matches | stored locations; coordinates/distance; via routes; suggested routes; imports | pure distance/coordinate policy -> reads -> writes | direction-specific distance semantics |
| `hotspots.service.ts` | 1,225 lines / 62 DB-call matches | CRUD/form; timing; parking; gallery; priority | pure timing/parking -> form orchestration -> writes | multi-table atomic saves |

## Constraints

- Keep the existing service as a compatibility facade initially.
- Pass Prisma transaction clients explicitly when logic crosses a transaction boundary.
- Avoid `forwardRef()` until dependency redesign is exhausted.
- Add characterization tests before moving code.
- Do not optimize a query without before/after counts/timings and a stable test.
- Do not remove indexes without actual definitions, foreign-key analysis, usage counters and rollback SQL.

## Retained extraction

`ItineraryRouteNormalizationService` now owns route-chain normalization, terminal-anchor recognition, broken-chain detection and duplicate movable-stop filtering. `ItinerariesService` remains the compatibility facade and continues to own distance optimization and route DTO construction. The new service has no Prisma or external-provider dependency and is registered by `ItinerariesModule`.

`TimelineOperatingHoursService` now owns timing-value formatting, daily operating-window summaries and closed-day policy checks used by `TimelineBuilder`. `TimelineBuilder` remains the compatibility facade and still owns the broader timeline assembly and hotspot sequencing. The helper has no Prisma or external-provider dependency and is instantiated by the existing manually constructed builder.

`LocationGeoPolicyService` now owns coordinate parsing, location-name normalization, case-insensitive string deduplication, duration text formatting and haversine distance calculation used by `LocationsService`. `LocationsService` remains the Prisma/API compatibility facade and the policy service has no database dependency.

`TimelineTravelDataService` now owns timeline hotspot/hotel location reads, hotel detail enrichment, stored-location coordinate resolution and distance projections. `TimelineBuilder` remains the compatibility facade and receives the existing shared `DistanceHelper` explicitly. Query shape and transaction ownership are unchanged; endpoint-level measurement remains pending.

`TimelineCandidateFeasibilityService` now owns read-only candidate admission and anchor-gap timing checks. It receives explicit policy and distance dependencies, returns the existing rejection reasons, and leaves timeline orchestration and persistence in `TimelineBuilder`.

`ItineraryGuideAssignmentService` now owns guide availability, assignment read projections/options and guide pricebook/GST resolution. `ItinerariesService` remains the compatibility facade, and the new provider is registered by `ItinerariesModule`. Guide writes and confirmation/cancellation transaction workflows remain in the facade until their own evidence-backed tier.

`ItineraryVehicleBuildStatusService` now owns vehicle-build run IDs, in-memory status, status-table lifecycle, readiness counts and DB/memory/derived status resolution. `ItinerariesService` remains the compatibility facade and delegates status lifecycle calls. Vehicle-build orchestration, vendor auto-selection and manual-fit persistence remain separate concerns for subsequent tiers.

`ItineraryVehicleBuildService` now owns vehicle-build orchestration, stage timeout/retry handling, permit sync, plan vehicle context and lowest-cost active vendor selection. `ItinerariesService` remains the compatibility facade and keeps the final `selectVehicleVendor` mutation behind an explicit callback. Transaction ordering, SQL shape and response contracts are unchanged.

`ItineraryPlanPersistenceService` now owns the basic-info create/update transaction and reusable-template persistence. It receives explicit engine dependencies and callbacks for route optimization, same-city optimization and plan-edit reads; `ItinerariesService` remains the compatibility facade. Transaction ordering and response envelopes are unchanged.

`ItineraryActivityWorkflowService` now owns activity add, preview, delete and all-hotspots preview workflows. It receives the existing hotspot engine and explicit callbacks for activity pricing, impact, timing and conflict policies; `ItinerariesService` remains the compatibility facade. Smart activity preview and anchored local rebuild remain separate because they share movement/rebuild helpers.

`ItinerarySmartActivityService` now owns smart activity preview/apply-preview and insertion together with hotspot-gap movement, anchored local rebuild and preview timeline assembly. It receives explicit time/conflict policy callbacks; `ItinerariesService` retains the shared hotspot-duration helper used by other workflows. This boundary intentionally exceeds the preferred helper size because splitting the transaction/rebuild sequence would obscure rollback and ordering semantics.

`ItineraryHotspotWorkflowService` now owns hotspot availability, anchor-aware availability, add and preview workflows. It receives explicit location/time/manual-preview callbacks and the existing hotspot engine; `ItinerariesService` remains the compatibility facade. Query shape and response contracts are unchanged.

`ItinerarySelectionWorkflowService` now owns hotel discovery/selection, bulk hotel persistence, vehicle-vendor selection, slab selection/auto-selection and vehicle-pricing rebuilds triggered by hotspot changes. It receives the existing route and vehicle engines plus hotel cache service; `ItinerariesService` remains the compatibility facade. Selection preservation, rate validation and response contracts are unchanged.

`ItineraryQuoteContextService` now owns plan edit reads, customer form projection and wallet balance resolution. It keeps route/via/vehicle/traveller ordering, agent display formatting, wallet fallback arithmetic and validation behavior intact; `ItinerariesService` remains the compatibility facade. Confirmation transaction logic remains in the facade.

`ItineraryConfirmationService` now owns the quotation confirmation transaction, supplier-booking normalization and confirmation persistence preparation. It receives explicit callbacks for the remaining facade-owned hotel-draft synchronization, wallet, date-format and draft-copy helpers; `ItinerariesService` remains the compatibility facade. Post-confirmation provider booking orchestration remains in the facade until its own boundary is measured.

`ItineraryHotelConfirmationSupportService` now owns selected-hotel draft synchronization, confirmation financial finalization and already-successful supplier-booking filtering. It receives explicit callbacks for confirmation normalization/key policies and wallet resolution; `ItinerariesService` remains the compatibility facade. External provider booking calls remain outside this database-oriented boundary.

`ItineraryHotelPrebookService` now owns TBO hotel prebook request/response normalization and fresh booking-code resolution. It receives explicit normalization and provider-filter callbacks plus the existing TBO, room-details and supplement services; `ItinerariesService` remains the compatibility facade. Provider booking orchestration remains separate.

`ItineraryHotelBookingFulfillmentService` now owns post-confirmation supplier booking dispatch, already-confirmed filtering, provider result aggregation and final financial status updates. It receives explicit confirmation/helper callbacks and the existing provider services; `ItinerariesService` remains the compatibility facade. External provider calls remain outside database transactions.

`ItineraryConfirmedPlanCopyService` now owns transaction-scoped copying of vehicles, routes, via routes, hotels, activities, guides, vendor eligibility, vehicle details and permit charges from draft to confirmed plans. It has no facade callbacks and receives the transaction client explicitly; `ItinerariesService` remains the compatibility facade.

`ItineraryCancellationService` now owns cancellation validation, cancellation transaction persistence, child-service cleanup, supplier cancellation dispatch, audit logging and notifications. It receives the existing Prisma and hotel-provider services directly; `ItinerariesService` remains the compatibility facade.

`ItineraryListingService` now owns agent/location filter reads and confirmed, cancelled and accounts itinerary listing queries. It receives Prisma directly and retains date parsing, role scoping, search, pagination and response projection behavior; `ItinerariesService` remains the compatibility facade.

`ItineraryVoucherReadService` now owns hotel and transport voucher read projections, including label/date/location/passenger formatting through explicit callbacks. It receives Prisma directly and preserves missing-plan validation and response contracts; `ItinerariesService` remains the compatibility facade.

`ItineraryManualHotspotMatrixService` now owns missing manual-hotspot route-matrix construction and its concurrency lock. It receives Prisma and explicit city/location policy callbacks, preserving matrix result codes and validation behavior; `ItinerariesService` remains the compatibility facade.

`ItineraryManualHotspotPreviewService` now owns manual-hotspot preview/batch preview, snapshot rollback, preview-cache state, Fit Here preview/auto-fit and Fit Here confirmation entry points. It receives explicit transaction/policy callbacks and preserves controller-compatible wrappers in `ItinerariesService`.

`ItineraryManualHotspotMutationService` now owns manual-hotspot add and batch-apply orchestration. It receives Prisma, the hotspot engine and explicit timing/cleanup/rebuild/retry callbacks, preserving add response projection and batch transaction behavior; `ItinerariesService` remains the compatibility facade.

`ItineraryManualFitMatrixPlanningService` now owns detour-optimized anchor inference, matrix insertion-gap resolution and low-priority-removal timeline reconstruction. It receives Prisma and explicit timeline/route helper callbacks, preserving matrix slot decisions, travel reconnection and removal sanitization; `ItinerariesService` remains the compatibility facade.

`ItineraryExactAnchorRebuildService` now owns exact-anchor sequential timeline reconstruction after removals. It receives the transaction client explicitly, keeps its bounded reconstruction cache local and accepts shared city, operating-window, travel-replica and route-leg policies through callbacks; `ItinerariesService` remains the compatibility facade.

`ItineraryLowPriorityRemovalService` now owns matrix-overflow removal planning, active-route removal evidence, removal sanitization and attempt snapshot helpers. It receives Prisma and the transaction client explicitly, preserves combination-search/greedy ordering and delegates shared timeline/snapshot policies through callbacks; `ItinerariesService` remains the compatibility facade.

`ItineraryMatrixSafeInsertionService` now owns matrix-safe manual-hotspot insertion, active-row checks, route-local persistence, removal application and post-apply validation. It receives Prisma and the transaction client explicitly, preserves timing/route/preview callbacks and keeps the facade as the compatibility boundary.

`ItineraryPreviewTimelineApplicationService` now owns insertion projection, destination-side baseline pruning, pivot backtracking cleanup and destination-side preview rebuilding. It preserves row ordering, timing labels, city-direction classification and preview response shapes through explicit callbacks; `ItinerariesService` remains the compatibility facade.

`ItineraryRouteLegCacheService` now owns OSRM leg runtime caching, TTL/key normalization, reverse-leg lookup, provider fallback, stored-leg reads and distance/duration fallback helpers. It receives the existing Prisma client and the facade's route-geometry callback; `ItinerariesService` remains the compatibility facade. Cache freshness and provider behavior are unchanged.

`ItineraryManualHotspotBatchService` now owns the manual-hotspot batch transaction workflow, including matrix-fit decisions, adaptive scheduling, preview reconstruction, priority-removal rescue and final response assembly. It receives the existing hotspot engine and explicit facade policy callbacks; `ItinerariesService` remains the compatibility facade. Transaction ordering, rollback behavior and response envelopes are unchanged.

`ItineraryManualInsertionFitService` now owns manual insertion-fit reads and slot selection, including city-endpoint, single-hotspot, route-between-map, destination-hotel and timing-aware matrix decisions. It receives explicit facade data/policy callbacks; `ItinerariesService` remains the compatibility facade. Existing raw query filters, route-fit ranking and response metadata are unchanged.

`ItineraryProgressivePriorityRemovalService` now owns progressive same-route removal planning, candidate auditing, timing evaluation, exact-anchor rescue and priority-ordered simulation attempts. It receives explicit facade timeline, city, timing and snapshot callbacks; `ItinerariesService` remains the compatibility facade. Priority protection, removal ordering and final response metadata are unchanged.

`ItineraryAdaptiveManualHotspotInsertionService` now owns adaptive manual-hotspot set insertion, baseline and removal-assisted optimizer sequencing, protected-priority handling, preview simulation state and removal explanations. It receives explicit facade candidate, optimizer, priority and exclusion callbacks; `ItinerariesService` remains the compatibility facade. The successful optimizer path, removal ordering and confirmation metadata are unchanged.

`ItineraryMatrixRescheduledPreviewService` now owns matrix-rescheduled preview assembly, source/anchor/hotel leg reconstruction, timing rescheduling, duplicate-travel cleanup and final arrival metadata. It receives explicit facade timeline, duration, route-leg and saved-rule callbacks; `ItinerariesService` remains the compatibility facade. Matrix preview ordering, timing labels and response mutation behavior are unchanged.

`ItineraryConfirmedItineraryDetailsService` now owns confirmed-itinerary booked-hotel projection, provider booking normalization, hotel/master enrichment, room/meal labels and availability metadata. It receives Prisma directly and the existing guide-assignment callback; `ItinerariesService` remains the compatibility facade. Provider precedence, booking labels, cancellation flags and response envelopes are unchanged.

`ItineraryMatrixRescheduledPreviewService` also now owns matrix baseline merge and selected-row insertion preparation. The merge policy shares the reschedule service's explicit timing, row-duration, travel-label and finalization callbacks, so matrix preview assembly remains one transaction-aware boundary rather than two facade helpers.

`ItineraryRouteTimingService` now owns route start/end validation, route-boundary recalculation, previous-day billing markers, timeline rebuild and post-rebuild parking/vehicle pricing refresh. It receives Prisma and the hotspot engine directly plus the existing vehicle-pricing callback; `ItinerariesService` remains the compatibility facade. Transaction ordering, timing validation and response metadata are unchanged.

`ItineraryManualFitTravelReplicaService` now owns manual-fit travel display normalization, duration/distance labels, hotel check-in travel insertion, saved hotel-leg fallback, source-to-hotspot leg resolution and hotspot-hotel map support. It receives explicit route/OSRM, duration, timeline and hotel-map callbacks; `ItinerariesService` remains the compatibility facade for legacy route helpers. Display fields, fallback precedence and matrix metadata are unchanged.

`ItineraryManualFitGeometryService` now owns route-coordinate parsing, route projection, OSRM geometry, selected/destination hotel endpoint resolution and hotspot-to-hotel leg fallback. It receives explicit city-classification and duration callbacks; `ItinerariesService` remains the compatibility facade for legacy geometry callers. Coordinate normalization, endpoint precedence and OSRM fallback behavior are unchanged.

`TimelineCandidatePolicyService` now owns timeline timing-window adapters, slot decisions, carry-forward candidate ordering/merge, route-chain policy, rejection classification and candidate-evaluation reporting. It receives the existing operating-hours, slot, rejection and route policy services directly plus explicit logging/city callbacks; `TimelineBuilder` remains the orchestration facade. Candidate ordering, closed-day filtering, route buffers and rejection metadata are unchanged.

`TimelineDataAccessService` now owns the timeline plan, route, active-hotspot and active-timing reads plus timing-map construction. `TimelineBuilder` remains the orchestration facade and continues to own global-settings hydration, closed-hotspot filtering and scheduling state. Active predicates, route ordering, query order and transaction ownership are unchanged; query/index/cache optimization remains pending measurement.

`TimelineDay1SourceFallbackService` now owns the bounded Day-1 source-city priority fallback selection. It receives explicit city-matching callbacks and Prisma transaction access, while `TimelineBuilder` remains the compatibility facade. Priority/distance ordering, exclusion semantics and query predicates are unchanged; source-city selectivity and coordinate-read performance remain unmeasured.

`TimelineRouteHotspotSelectionService` now owns route-local hotspot context loading, timing-aware source/en-route/via/destination bucket selection, route-chain matching, distance ranking, exclusion handling and final candidate projection. It receives the shared distance helper and explicit route/city/coordinate/logging callbacks; `TimelineBuilder` remains the compatibility facade. Bucket precedence, ordering and response metadata are unchanged; query/index/cache optimization remains pending measurement.

`TimelineArrivalHotelDecisionService` now owns arrival-window evaluation, hotel-first/distance branching, early/late-arrival clock policy, report-cutoff suppression and Day-1 branch diagnostics. It receives the shared distance helper and explicit hotel/data/policy/logging callbacks; `TimelineBuilder` remains the compatibility facade and owns row insertion. Absolute timing, policy flags and route-clock updates are unchanged; decision and hotel-coordinate performance remain pending measurement.

`TimelineHotelFirstInsertionService` now owns the optional Day-1 hotel travel/check-in/rest row sequence, including check-in clamping and state updates. It receives the existing hotel and refreshment builders plus an explicit booking-rule logger; `TimelineBuilder` remains the compatibility facade. Eligibility gates, row order, time labels and transaction client usage are unchanged; insertion and provider latency remain pending measurement.

`TimelineNonHotelCutoffService` now owns the distance-backed latest non-hotel sightseeing cutoff calculation. It receives the shared distance helper and explicit city-normalization policy; `TimelineBuilder` remains the compatibility facade. Direct/final-route bypasses, buffer subtraction and formatted cutoff values are unchanged; provider/cache performance remains pending measurement.

`TimelineRouteHotspotPlanningService` now owns route-hotspot selection planning, via-route classification, Day-1 fallback selection, destination reservation guards and same-city carry-forward expiry. `TimelineBuilder` remains the compatibility facade and keeps closed-day filtering, candidate admission, travel rows, persistence and final timeline assembly. The service receives the transaction client and existing policy callbacks explicitly; query shape, ordering and transaction ownership are unchanged.

`TimelineManualPlacementOrderingService` now owns route-scoped preview membership, persisted route-order application, desired same-city movable ordering and manual hotspot merging. `TimelineBuilder` remains the compatibility facade and receives the returned ordering maps for later scheduling. The service has no database dependency; selection precedence, deterministic ordering and booking-rule diagnostics are unchanged.

`TimelineDestinationReservationService` now owns destination-loopback reservation feasibility, destination-bucket filtering, source fallback, empty-route rescue and reservation diagnostics. `TimelineBuilder` remains the compatibility facade and keeps explicit via/direct cleanup, carry-forward merging, matrix augmentation and timeline scheduling. The service receives transaction and policy callbacks explicitly; query shape, ordering and transaction ownership are unchanged.

`TimelineCarryForwardAttachmentService` now owns source-fallback-only classification and same-city carry-forward attachment. `TimelineBuilder` remains the compatibility facade and keeps matrix augmentation, candidate feasibility and timeline row scheduling. The service has no database dependency; suppression gates, merge arguments and scheduler-facing flags are unchanged.

`TimelineMatrixAutobuildService` now owns feature-flagged between-hotspot matrix augmentation, route-fit/corridor filtering, matrix scoring, timing admission and candidate merge. `TimelineBuilder` remains the compatibility facade and keeps candidate reordering, feasibility scheduling, travel rows and persistence. The service receives the transaction client and existing data/policy callbacks explicitly; the route-hotspot read, between-map query shape and transaction ownership are unchanged.

`TimelineCandidateReorderingService` now owns manual/positive-priority protection and matrix-score/distance ordering of selected candidates. `TimelineBuilder` remains the compatibility facade and keeps route scheduling, operating-hour evaluation, travel rows and persistence. The service has no database dependency; ordering and logging behavior are unchanged.

`TimelineDay1CandidateGateService` now owns Day-1 strict priority/filler filtering, terminal-arrival source suppression, duplicate protection and rejection diagnostics. `TimelineBuilder` remains the compatibility facade and keeps cutoff evaluation, hotspot-master reads, travel calculation, operating-hour scheduling and timeline row mutation. The service has no database dependency; movement exceptions and rejection ordering are unchanged.

`TimelineDay1CutoffMasterService` now owns Day-1 source/via/destination cutoff evaluation and prefetched hotspot-master admission. `TimelineBuilder` remains the compatibility facade and keeps coordinate resolution, travel calculation, projected-arrival checks, operating hours and timeline mutation. The service has no database dependency; cutoff thresholds, loopback bypass and rejection metadata are unchanged.

`TimelineDay1TravelProjectionService` now owns Day-1 source-coordinate fallback, hotspot travel-duration calculation, absolute/wrapped visit-time projection and route-end/projected-arrival rejection logging. `TimelineBuilder` remains the compatibility facade and keeps operating-hours evaluation, waiting policy and timeline row mutation. Provider callbacks and transaction ownership are unchanged; coordinate fallback, travel-provider and projected-arrival latency remain pending measurement.

`ItineraryGuideAssignmentWriteService` now owns draft guide-assignment validation, eligible-guide cost resolution, guide-row upsert/delete and per-route/all-itinerary slot-cost persistence. `ItinerariesService` remains the compatibility facade and delegates the existing controller contract; the existing guide policy service owns availability and cost rules. Transaction ordering, payload normalization and response projection are unchanged; guide-cost query latency and slot-row volume remain pending measurement.

`ItineraryConfirmedGuideAssignmentService` now owns confirmed-plan guide/slot-cost hydration, draft-slot-cost backfill and deterministic guide assignment response projection. `ItinerariesService` remains the compatibility facade and delegates the existing confirmed-guide callback contract. Query predicates, ordering, fallback dates and labels are unchanged; confirmed-guide read latency and hydration fan-out remain pending measurement.

`ItineraryConfirmedGuideCancellationService` now owns confirmed guide-slot cancellation validation, cancellation-row creation, slot refund/charge calculation, route/itinerary cancellation aggregation and confirmed-plan status updates. `ItinerariesService` remains the compatibility facade and supplies the existing cancellation-audit callback. Transaction ordering, financial rounding, status transitions and response fields are unchanged; cancellation transaction latency and aggregate fan-out remain pending measurement.

`ItineraryManualFitAttemptStoreService` now owns manual-fit preview attempt table creation, raw attempt persistence, validation, short-lived in-memory lookup and deletion. `ItinerariesService` remains the compatibility facade for the existing manual-fit helper callbacks. SQL text, expiry fallback, cache-before-database behavior and parse-failure logging are unchanged; table setup, raw query and payload-cache performance remain pending measurement.

`ItineraryHotspotDeletionService` now owns route-hotspot identity resolution, dependent activity/timeline cleanup, route exclusion updates, hotspot rebuild, parking rebuild and vehicle-pricing refresh sequencing. `ItinerariesService` remains the compatibility facade and supplies the existing pricing callback. Transaction timeout, deletion predicates, rebuild order and response fields are unchanged; deletion fan-out and rebuild latency remain pending measurement.

`ItineraryActivityAvailabilityService` now owns active activity catalog reads, time-slot loading and plan-specific pricing projection. `ItinerariesService` remains the compatibility facade and supplies the existing activity-pricing callback. Activity ordering, slot ordering, response fields and pricing semantics are unchanged; activity/slot fan-out and pricing latency remain pending measurement.

`ItineraryInvoiceReadService` now owns confirmed itinerary pluck-card and invoice presentation reads, including customer/settings projection, GST labels, hotel/vehicle/service line-item assembly and totals. `ItinerariesService` remains the compatibility facade for the existing controller methods. Query predicates, parallel read ordering, financial arithmetic and response fields are unchanged; confirmed-plan read fan-out, row volume and response latency remain pending measurement.

`ItineraryManualFitTimelinePolicyService` now owns low-priority timeline invariant validation, removed-row sanitization/pruning, retry classification, exact-anchor fit normalization and planned-removal detection. `ItinerariesService` remains the compatibility facade and preserves the callback method names consumed by manual-fit workflows. Removal predicates, ordering metadata, diagnostics and anchor response fields are unchanged; policy CPU and preview row-volume effects remain pending measurement.

`ItineraryMatrixPreviewTimelinePolicyService` now owns matrix-preview timeline finalization, travel-label normalization, placeholder time-range repair, duration parsing, preview label formatting and matrix-order invariant diagnostics. `ItinerariesService` remains the compatibility facade and supplies the existing time/duration callbacks. Row ordering, time formatting, duplicate suppression, travel labels and debug assertion behavior are unchanged; preview-policy CPU and response latency remain pending measurement.

`ItineraryManualFitRemovalExplanationService` now owns manual-fit duration/time display helpers, attempted-attraction enrichment, removal explanations, priority summaries, authoritative removed-hotspot selection and changes-required response projection. `ItinerariesService` remains the compatibility facade and supplies existing preview-time/closing-time callbacks. Removal reason precedence, evidence fields, priority ordering and response envelopes are unchanged; explanation assembly CPU and payload size remain pending measurement.

`ItineraryManualFitRoutePolicyService` now owns route-fit labels/ranks, valid-slot and empty-route eligibility rules, matrix-build suggestions, location normalization and source/destination city-context classification. `ItinerariesService` remains the compatibility facade for manual insertion and route-fit callbacks. Fit-type precedence, city classification, suggestion fields and response metadata are unchanged; policy CPU and route-fit decision latency remain pending measurement.

`ItineraryManualFitTravelReplicaService` now also owns main-timeline travel-replica indexing, travel-label normalization, distance parsing, duration fallback selection, check-in hotel-name extraction and travel-replica lookup. `ItinerariesService` remains the compatibility facade and supplies existing timeline-duration callbacks. Replica key precedence, fallback duration rules and display labels are unchanged; map-build CPU, fallback frequency and lookup latency remain pending measurement.

`ItineraryManualFitTravelReplicaService` now also owns saved-rule travel-location classification, hotspot endpoint projection and source-to-hotspot, hotspot-to-hotspot and hotspot-to-route-hotel leg resolution. It retains the existing distance helper and route/hotel callbacks; saved-rule buffer inclusion, endpoint predicates, travel-location type and response fields are unchanged. Saved-rule lookup latency and distance-helper performance remain pending measurement.

`ItineraryManualFitRouteMatrixPersistenceService` now owns hotspot-place upsert/identity resolution, route-between-map persistence, route-between rejection lookup, geometric distance/progress projection and source-city OSRM hotspot-anchor discovery. `ItinerariesService` remains the compatibility facade and supplies existing geometry, normalization and OSRM callbacks. Transaction ownership, SQL predicates, mirrored route keys, route-fit classification and source-anchor ordering are unchanged; raw query latency and OSRM performance remain pending measurement.

`ItineraryManualFitOperatingHoursService` now owns manual-fit timing-row enrichment, operating-window parsing, attempted-row conflict evaluation, opening-window adjustment, selected-closing overflow and selected-hotspot operating-hour conflict marking. `ItinerariesService` remains the compatibility facade and supplies the existing preview-time and formatting callbacks. Timing predicates, overnight-window handling, wait calculations, conflict reason fields and Prisma read predicates are unchanged; timing-row fan-out and policy latency remain pending measurement.

`ItineraryManualFitValidationService` now owns selected manual priority resolution, insertion-slot insight projection and final manual-hotspot schedule validation. `ItinerariesService` remains the compatibility facade and supplies distance, operating-hours evaluation and route-end overflow callbacks. Feasibility thresholds, conflict reasons, relaxed off-route handling and response fields are unchanged; policy CPU and validation latency remain pending measurement.

`ItineraryManualFitScheduleAttemptService` now owns travel-metric projection, protected-priority impact detection, exact-anchor attempt construction and deterministic schedule-attempt comparison. `ItinerariesService` remains the compatibility facade and supplies distance, timeline-anchor, time-parsing and attempt-explanation callbacks. Attempt category precedence, overlap detection, protected-priority reasons and readiness fields are unchanged; metric and comparison CPU remain pending measurement.

`ItineraryManualFitCandidateSimulationService` now owns one manual insertion-position simulation, including exact-anchor recovery, preview enrichment, schedule-state projection, score inputs and rejection reasons. `ItinerariesService` remains the compatibility facade and supplies rebuild, timeline, operating-hours, priority, scoring and timing callbacks. Transaction scope, candidate result fields, exact-anchor failure reasons and optimizer diagnostics are unchanged; per-position query fan-out and simulation latency remain pending measurement.

`ItineraryManualFitCandidateSearchService` now owns insertion-position ordering, candidate simulation orchestration, slot-insight attachment, best-candidate selection and cluster-strategy comparison. `ItinerariesService` remains the compatibility facade and supplies route reads, insertion policies, simulation callbacks and rebuild callbacks. Position precedence, destination/source ordering, selected-position rebuild and optimizer log fields are unchanged; candidate-count and strategy-search latency remain pending measurement.

`ItineraryManualFitCandidateDataService` now owns route-hotspot, hotspot-master and active-timing reads plus timing-window formatting, manual priority projection and candidate-row assembly. `ItinerariesService` remains the compatibility facade and supplies normalization, duration and classification callbacks. Active predicates, timing order, UTC formatting, priority semantics and candidate response fields are unchanged; read fan-out and query latency remain pending measurement.

`ItineraryManualHotspotRowService` now owns route exclusion-list maintenance and manual hotspot row reuse, stale-row retirement and placeholder creation. `ItinerariesService` remains the compatibility facade and supplies row-duration calculation callbacks. Exclusion membership, active/deleted predicates, stale-row handling, placeholder timestamps and response fields are unchanged; write frequency and row-persistence latency remain pending measurement.

`ItineraryManualHotspotScheduleStateService` now owns manual-row schedule-state reads and operating-window membership checks. `ItinerariesService` remains the compatibility facade and supplies duration and overlap callbacks. Route/date/timing predicates, weekday conversion, overnight-window handling, permissive no-date/no-timing fallback and conflict filtering are unchanged; schedule-state read latency, timing fan-out and overlap-query cost remain pending measurement.

`ItineraryManualHotspotRowTimingService` now owns stale manual-row retirement and timed-row activation/reuse. `ItinerariesService` remains the compatibility facade and supplies ID normalization, duration and UTC-duration-date callbacks. Active-row predicates, duration validation, newest-row reuse, duplicate retirement, timestamps, conflict reset and response identity are unchanged; stale-row scan volume, write amplification and activation latency remain pending measurement.

`ItineraryManualHotspotOverlapService` now owns active manual-row overlap checks and non-overlapping-row selection. `ItinerariesService` remains the compatibility facade and the schedule-state service supplies the existing callback boundary. Route/plan/item/deleted predicates, time conversion, invalid-window rejection, conflict-row exclusion and half-open overlap semantics are unchanged; overlap query latency and row fan-out remain pending measurement.

`ItineraryManualHotspotConflictService` now owns forced manual-row conflict update/create persistence after confirmation. `ItinerariesService` remains the compatibility facade and supplies the UTC-duration-date callback. Existing-row lookup, route-time fallback, order allocation, conflict reason, timestamps and response success semantics are unchanged; conflict-row lookup latency and write amplification remain pending measurement.

`ItineraryRouteHotspotRebuildService` now owns the manual/exclusion reset transaction and plan-wide hotspot rebuild workflow. `ItinerariesService` remains the compatibility facade and supplies same-city optimization and vehicle-pricing callbacks. Route validation, date-enriched existing-hotspot snapshot, manual/activity retirement, exclusion clearing, rebuild timeout, parking/pricing side effects and skipped-route response fields are unchanged; rebuild query fan-out and transaction duration remain pending measurement.

`ItineraryHotelCancellationService` now owns confirmed-hotel cancellation reads, day-level charge projection and transactional cancellation/refund accounting. `ItinerariesService` remains the compatibility facade. Plan/hotel/room predicates, response fields, audit fallback, soft-delete order and financial decrement/increment semantics are unchanged; cancellation read fan-out and transaction duration remain pending measurement.

`ItineraryHotelRoomCategoryService` now owns TBO-backed room-category projection and room-selection update/create persistence. `ItinerariesService` remains the compatibility facade. Plan/route/hotel/group predicates, available-room projection, preferred-slot fallback, TBO pricing, meal flags and room response fields are unchanged; TBO latency, room-row fan-out and selection write latency remain pending measurement.

`ItineraryRouteOptimizationService` now owns the active PHP-parity route-ordering policy, permutation/annealing helpers, stored-distance reads and route DTO projection. `ItinerariesService` delegates optimization to the new service while retaining the previous helper block as an untouched compatibility copy because its mojibake log literals cannot be safely removed through the current patch path. Ordering, distance predicates and response mapping are unchanged; optimizer query latency and candidate CPU remain pending measurement.

`ItineraryActivityImpactService` now owns activity-duration impact simulation, downstream hotspot projection, optional-row removal planning and rollback-only reroute simulation. `ItinerariesService` remains the compatibility facade and supplies time conversion callbacks. Activity/hotspot/route predicates, priority warning fields, route-end decisions and rollback marker behavior are unchanged; activity impact query fan-out and simulation latency remain pending measurement.

`ItineraryTransportFormattingService` now owns transport voucher date/range formatting, passenger labels, voucher numbers, time/location display normalization, HTML decoding and flight-detail parsing. `ItinerariesService` remains the compatibility facade and supplies the existing `formatTime` callback. Display fallbacks, date formatting, location normalization, JSON/raw parsing and response fields are unchanged; formatting CPU, malformed-payload frequency and voucher projection latency remain pending measurement.

`ItineraryActivityPricingService` now owns plan/route passenger context, nationality classification, dated activity pricebook lookup, day-one fallback and activity price projection. `ItinerariesService` remains the compatibility facade and passes the existing Prisma or transaction client through unchanged. Pricebook predicates, date fallback, rate selection, unit/per-adult precedence and response fields are unchanged; pricebook read latency, fallback frequency and per-activity availability fan-out remain pending measurement.

`ItineraryActivityTimingPolicyService` now owns UTC minute conversion, display time formatting, minute addition and activity time-slot conflict projection. `ItinerariesService` remains the compatibility facade for activity workflow, smart activity, impact simulation and transport callbacks. UTC semantics, warning text, slot-fit precedence and response fields are unchanged; timing-policy CPU and conflict-evaluation frequency remain pending measurement.

`TimelineBuildContextService` now owns plan/route hydration, scoped-route selection, previous-route indexing, active-hotspot/timing hydration, hotspot lookup-map construction, global-settings preload and permanently-closed-hotspot filtering. `TimelineBuilder` remains the scheduling facade and receives the same context maps and rows. Query predicates, ordering, global-settings freshness, closed-day filtering and evidence logging are unchanged; context-read fan-out and prefilter latency remain pending measurement.

`ItineraryDetailsTimelinePresentationService` now owns confirmed-timeline chronology normalization and semantic travel-label reconstruction. `ItineraryDetailsService` remains the compatibility facade and delegates the same segment arrays and fallback hotel label. Attraction time-source protection, break/travel chronology rules, adjacent-stop label precedence and response fields are unchanged; presentation CPU and segment-count effects remain pending measurement.

`ItineraryHotelDetailsCacheService` now owns TBO hotel-detail and room-detail cache keys, TTL checks, bounded eviction, quote invalidation and cache statistics. `ItineraryHotelDetailsTboService` remains the provider/search facade and preserves its public cache invalidation methods. Cache freshness, key precedence, eviction limit and stats fields are unchanged; hit rate, eviction frequency and provider-call reduction remain pending measurement.

`hotel-list-normalization.ts` now owns the pure frontend hotel display policies for meal-plan labels/codes, money/date formatting, HTML-safe inclusion lists and hotel-star category normalization. `HotelList.tsx` remains the compatibility facade for selection state, room/rate presentation and voucher actions. Supplier fallback precedence, formatting output and existing category parsing behavior are unchanged; normalization CPU and rendered component size remain pending measurement. Implementation commit: `dvi_frontend` `5841480`.

`vehicle-pricing-policy.ts` now owns pure local vehicle numeric normalization, time-limit signature parsing, deterministic slab ordering, usage coverage and selected-slab upgrade policy. `vehicle-calculation.helpers.ts` remains the compatibility facade for location reads, toll/parking, distance/duration and vehicle-cost orchestration. Slab precedence, fallback selection, upgrade flags and numeric coercion are unchanged; pricing-policy CPU and local pricebook query fan-out remain pending measurement.

`vendor-pricebook-policy.ts` now owns pure vendor pricebook normalization for outstation kilometre signatures, local time-limit signatures and soft-delete marker allocation. `VendorsService` remains the compatibility facade for vendor CRUD, pricebook reads/writes, branch/location resolution and vehicle operations. Title fallback, explicit-value precedence and deletion-marker sequencing are unchanged; policy CPU and pricebook query fan-out remain pending measurement.

`ItineraryInputNormalizationService` now owns pure itinerary input normalization for CSV/date values, route-family quote parsing, mixed-array/string cleanup, meal-plan inference and manual-hotspot ID normalization. `ItinerariesService` remains the compatibility facade and passes the same callback arguments to collaborating workflows. Fallback precedence, date formatting, duplicate suppression and positive-ID filtering are unchanged; normalization CPU and payload-shaping latency remain pending measurement.

`itinerary-service-collaborator-wiring.ts` now owns the constructor-time callback graph that connects the itinerary facade to its policy services. `ItinerariesService` remains the compatibility facade and supplies the same instance-bound callbacks; callback names, service registration, transaction boundaries and legacy helper ownership are unchanged. Wiring initialization time and callback fan-out remain pending measurement.

`TimelineBuilder` now delegates route selection, day-one fallback, travel-data lookup, anchor policy and candidate-feasibility calls directly to their owning services; redundant bottom-of-file compatibility wrappers were removed. Query predicates, service argument order, route timing and candidate response fields are unchanged; direct delegation overhead and remaining orchestration CPU remain pending measurement.

`ItineraryDetailsTimeRangePolicyService` now owns display-time parsing, overnight range ordering, travel-end derivation and display-duration formatting. `ItineraryDetailsService` remains the compatibility facade and delegates the existing internal helper calls. UTC duration handling, 12-hour labels, overnight ordering and response fields are unchanged; policy CPU and details projection latency remain pending measurement.

`ItineraryDetailsDisplayFormattingService` now owns UTC-safe date-only formatting, created-on labels, trip/time display, duration labels and two-digit padding. `ItineraryDetailsService` remains the compatibility facade and delegates the existing formatting calls. Wall-clock semantics, locale-shaped labels and response fields are unchanged; formatting CPU and details projection latency remain pending measurement.

`ItineraryDetailsRouteHotelMapService` now owns draft/confirmed route-hotel reads, local/TBO master enrichment, route-code fallback and vehicle-only hotel labeling. `ItineraryDetailsService` remains the compatibility facade and receives the same route hotel map. Query predicates, selected fields, precedence and response fields are unchanged; hotel lookup fan-out and map-construction latency remain pending measurement.

`ItineraryDetailsTravelSemanticsService` now owns chronological attraction-sequence construction, travel-row origin/destination reconstruction and prior-hotel check-in origin selection. `ItineraryDetailsService` remains the compatibility facade and supplies existing formatting, hotel and conflict callbacks. Conflict suppression, semantic IDs, fallback origins and travel fields are unchanged; semantic CPU and timeline projection latency remain pending measurement.

`ItineraryDetailsRouteHotspotDataService` now owns per-route hotspot SQL hydration, chronological sorting, hotspot master/name indexing, timing grouping and gallery URL indexing. `ItineraryDetailsService` remains the compatibility facade and consumes the same route rows and maps. Query predicates, selected fields, ordering and response fields are unchanged; route read fan-out and map-construction latency remain pending measurement.

`ItineraryDetailsEntryTicketCostService` now owns plan-scoped entry-ticket cost hydration, numeric normalization and route-hotspot grouping. `ItineraryDetailsService` remains the compatibility facade and consumes the same grouped map. Active predicates, ordering, positive-ID filtering and response fields are unchanged; entry-ticket row volume and grouping CPU remain pending measurement.

`ItineraryLatestDataTableService` now owns latest-itinerary DataTable parameter normalization, role/search filtering, plan/user/staff/agent hydration, confirmed-plan exclusion and row projection. `ItineraryDetailsService` remains the compatibility facade and supplies Prisma plus existing date/display policies. Search predicates, pagination, labels and response fields are unchanged; DataTable query fan-out and projection latency remain pending measurement.
